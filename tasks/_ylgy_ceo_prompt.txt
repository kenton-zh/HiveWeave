[测试程序] 平台功能回归测试 — 2026-07-28 基线  你是本次回归测试的总负责人。平台（HiveWeave 本身，不是你们的项目代码）最近落地了一批新功能， 需要你组织团队做端到端实测并交付测试报告。以下为必读背景与强制流程。  == 第 0 步：预检（阻塞性，先做）== P0-1. 用 message_user 向用户确认两项平台配置是否已在 Settings 完成：   a) 多模态模型槽位（vision primary / backup）—— look_at_image 与 assert_visual 依赖它；   b) 模型分级槽位（management / executor 各自的 primary / backup）。   用户确认前，视觉类用例标记 blocked，其余测试先行，不得停摆等待。 P0-2. 对每名直属下级各调用一次 check_agent_progress，记录返回内容（这是你的新工具，调用本身即测试）。  == 第 1 步：组织 == 盘点 roster。本程序需要：一名测试组长（中层 builder coordinator，你的直属下级，负责拆派与汇总）， 至少一名 executor（环境搭建），至少一名 QA 工程师。缺员走 HR 招聘： QA 必须绑定内置技能 h5-game-qa（bind_skill，slug 全名）；executor 岗位名遵守「模块短名+工种」。 招聘成功后按纪律向请求方 send_message 报告到岗。  == 第 2 步：测试里程碑（派给测试组长拆派，禁止越级直派叶子）==  M1 H5 游戏 QA 线（核心，优先级最高）   1) executor 用 bash 把平台 fixture 复制进项目工作区：      源 D:\PC_AI\Project\HiveWeave\apps\hiveweave-py\fixtures\h5_jump_demo\      目标 <项目工作区>\h5_jump_demo\，然后用 start_dev_server 起静态服务（禁止裸 vite 撞 5173）。   2) QA 走完整工具链：browse(goto "http://127.0.0.1:<端口>/?hw_test=1")      → game_run_case(action="probe") → action="list" → action="run", caseId 逐个跑      （jump_cross_gap / fall_in_pit）。   3) 每个 case 必须过双闸门：返回 codePass=true，且 QA 对截图调用 assert_visual      （screenshotPath + observed + criteria + verdict）判定 pass。缺一不算过。   通过标准：两个 case 双闸门全过；证据 = 截图文件路径 + assert_visual 结论 + codePass 值。  M2 视觉工具线   1) look_at_image：任一 agent 对 M1 产出的截图做无状态识图，验证返回描述与画面一致。   2) browse js/eval：QA 用 browse 对 demo 页执行 JS 读取 window.__HW_TEST__ 状态，      验证 browse 的 JS 求值能力（新能力）。   通过标准：两个工具均返回有效结果；若 vision 槽位未配置导致报错，记录报错原文并标 blocked。  M3 权限与门禁线（含负向用例——「被拒绝」才是通过）   1) CEO 自测 DOC_WRITE（你亲自做）：write_file 写 deliverables/regression-notes.md 应成功；      再用 write_file 尝试写任意 .py 或 .ts 文件，必须被硬拒——记录 deny 提示原文      （应明确区分 CEO 与 builder coordinator 两种口径）。   2) 审查方证据硬闸：安排一个代码类小任务走完整 submit→review 流，QA 在【本人未跑过      run_tests】的情况下直接 approve，必须被拒；然后 QA 真正跑过测试再 approve，应放行。   3) UNREPLIED_ASKS 硬门：测试组长对一名下级 ask_agent（expect_report），要求该下级      【只在 assistant 文本里回答、不调 send_message 类工具】，观察其回合出口被拦截续跑；      随后正常用工具回复后应能收口。   4) 任务去重：测试组长连续创建两个标题与 assignee 完全相同的任务，观察结构化去重生效。  M4 组织与韧性线   1) HR 调 list_available_skills 搜索任意关键词：即使外网 skills.sh 不可达，也必须返回      内置技能列表（SkillHub 降级），记录耗时与来源。   2) executor 全部落账后，用 git_worktree_list / git_worktree_status 核对每人 worktree 绑定；      审查一律读 worktree 目录，不看项目根 main。   3) reassign 锁定：把一个已 claimed 任务 reassign 给另一人，确认原实现者被锁、      新 assignee 落账 claimed。   4) VERIFY 独立化：M3-2 任务 approve+merge 后，确认自动 spawn 的 VERIFY 任务      既不落原实现者也不落合并人。  M5 观察项（不阻塞，看到就记录）   模型 429 时是否发生同 tier backup 切换；stall break 后是否有宽限而非直接停泊；   沉默看门狗是否只对在班 agent 触发；agent 引用文件/花名前是否先工具验证（接地纪律）。  == 全程纪律 == - 你只派活给直属测试组长；需要回信用 ask_agent 或 expect_report=true，FYI 用 notify_agent。 - 每个用例的结论必须挂结构化证据（工具返回摘要、截图路径、attestation 记录），   不接受「我觉得过了」。证据不足你应 reject 打回。 - 审查/批准代码任务前，确认审查方本人持有该任务新鲜 test_run attestation。  == 第 3 步：交付 == 1) 你亲自 write_file 写 deliverables/platform-regression-2026-07-28.md：    用例清单与通过/失败/blocked 状态、每项证据指针、失败项复现步骤、修复建议。 2) 终审后 message_user 三段式汇报：总体结论 / 失败与 blocked 项（含严重度）/    需要用户人工处理的平台配置项。 3) commit_turn(done_slice) 收口。