# 功能契约 XX：模块名称

> **本文件是功能契约（层 1），用 spec 语言描述模块的行为契约，不引用实现代码。**
> **任何 AI 工具实现 Python 版本时，必须满足此契约中的所有要求。**

## 元信息

| 项 | 值 |
|---|---|
| 模块编号 | XX |
| 模块名称 | — |
| Elixir 源码 | `apps/hiveweave/lib/hiveweave/...` |
| TS 参考源码 | `packages/.../src/...` |
| OpenCode 参考源码 | `D:\PC_AI\Project\opencode\packages\.../src/...` |
| 状态 | 草稿 / 待确认 / 已确认 |

## 功能概述

（一段话描述这个模块的核心职责。是什么，不是怎么实现。）

## 接口契约

### 输入（Consumes）

（这个模块接收什么输入：函数签名、API 端点、事件、消息格式。用 spec 语言，不是代码。）

| 输入 | 来源 | 格式 | 说明 |
|---|---|---|---|
| — | — | — | — |

### 输出（Produces）

（这个模块产出什么：返回值、事件、副作用产出。）

| 输出 | 目标 | 格式 | 说明 |
|---|---|---|---|
| — | — | — | — |

### 副作用

（DB 写入、文件操作、网络请求、状态变更、广播事件。）

| 副作用 | 触发条件 | 目标 | 说明 |
|---|---|---|---|
| — | — | — | — |

## 核心流程

（用编号步骤描述核心流程。每步说"做什么"，不说"怎么做"。）

```
1. ...
2. ...
3. ...
```

## 状态机（如适用）

（如果有状态转换，列出状态、转换条件、转换动作。）

| 当前状态 | 触发条件 | 目标状态 | 动作 |
|---|---|---|---|
| — | — | — | — |

## 错误处理

| 错误场景 | 处理方式 | 重试策略 | 升级策略 |
|---|---|---|---|
| — | — | — | — |

## 常量引用

（引用 `constants.md` 中的相关常量，只列常量名和值，不重复解释。）

| 常量 | 值 | 所在章节 |
|---|---|---|
| — | — | — |

## 已知问题

（引用 `known-issues.md` 中的相关问题编号，说明迁移时如何处理。）

| 问题编号 | 说明 | Python 迁移处理 |
|---|---|---|
| — | — | — |

## Python 实现建议

（技术选型、架构建议、对应库。不是代码，是指导。）

- **框架/库**：—
- **架构模式**：—
- **注意事项**：—

## 验收标准

（如何判断 Python 实现行为等价于 Elixir/TS。可测试的断言。）

- [ ] —
- [ ] —

## 并行对比测试方案

（Phase 4 并行验证时如何测试此模块。）

| 测试场景 | Elixir 行为 | Python 预期行为 | 验证方法 |
|---|---|---|---|
| — | — | — | — |

---

> **填写规则**：
> 1. 用 spec 语言，不用代码语言。说"做什么"，不说"怎么做"。
> 2. 所有常量值引用 `constants.md`，不在此处重复定义。
> 3. 所有已知问题引用 `known-issues.md`，不在此处重复描述。
> 4. 每个契约必须经过用户确认后才能标记为"已确认"。
> 5. "Python 实现建议"是建议不是约束，实现者可以根据情况调整，但必须满足验收标准。
