# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
.next/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# PID files (runtime process IDs — machine-specific, must not be tracked)
*.pid

# Logs
*.log
# 轮转备份代（util/log_rotate.py 产出 .1/.2…）。⚠ `*.log` 匹配不到
# `server.out.log.1`，不加这条每代备份都会变成未跟踪文件、并混进 OCR 审计。
*.log.[0-9]*
npm-debug.log*
# ⚠ 同族的第二处（2026-09-24 实证）：`tasks/*.output` 匹配不到 `x.output.1`，
# `tasks/backend-20260923-171729.output.1` 就是这样浮成未跟踪文件的。
tasks/*.output
tasks/*.output.*

# Docker
.docker/

# SQLite
*.db
*.db-journal
*.db-wal
*.db-shm

# Test coverage
coverage/

# Temp
tmp/
temp/
tmp_*.py
tmp_*.txt
.tmp_*
_research/
.research/
.uploads/

# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Temp patch/fix scripts
_probe*.py
_fix*.mjs
_fix*.py
_fix*.cjs
_fix*.js
_patch*.py
_patch*.mjs
_patch*.cjs
_p[0-9]*.cjs
_pi.mjs
_apply_*.py
_apply_*.mjs
apply-app-patch.mjs
_ceo_*.txt
_t.txt
_inspect*.js
_inspect*.py
tasks/_diag_*
# scripts 下的**下划线前缀** = 一次性探针（mock 形状 / 落点标定 / 截图），跑完即弃。
# ⚠ 用结构性规则而不是继续补后缀：上面那串 `_probe*.py` / `_fix*.mjs` 逐条列举的
# 写法只覆盖已见过的后缀 —— 2026-09-24 就是 `.mjs` 探针（5 个）漏网浮成未跟踪的。
apps/web/scripts/_*

# Chat history exports (other projects)
*_chat_history.json
*_chat_history.md

# Empty temp files
SUMMARY.md
create_summary.py

# Temp debug files
check_db_tmp.ts

# Tool caches
.playwright-cli/
.turbo/
.workbuddy/

# Test fixtures
test-*.json
test_*.json

# Build diagnostics
tsc_err.txt
tsc_out.txt
*.tsbuildinfo
.npmrc

# Temp test artifacts
test_main_started.txt
test_output.txt
test-screenshots/
test-*/
test_*/
frontend-e2e-test.mjs
apps/web/frontend-e2e-test.mjs

# Cross-platform AI project memory (local only; do not push)
AI_MEMORY.local.md
*.local.md

# AI-agent instruction / tool docs (local working guidance, not project source)
AGENTS.md
CLAUDE.md
.claude/

# Local experiment/observation snapshots (复盘用，不入库)
observations/

# Local self-contained HTML analysis reports (bundle vendored echarts/mermaid/fonts)
reports/

# One-off per-project DB audit scripts (external absolute paths, snapshot-bound)
tasks/_tmp_*
tasks/diag_*.py
tasks/dsh*.py
tasks/dsh-watch-state.json
scripts/_tmp_*
apps/hiveweave-py/scripts/mechanism_attribution.py
apps/hiveweave-py/scripts/strip_reserved_tags.py

# Test postmortems / audit reports (process artifacts, local only)
deliverables/
audit_task_id_normalization.md

# Local self-contained competitive-analysis report site (vendored fonts/js)
hiveweave-competitive-analysis-2026/

# Misc
after-select.yaml
erl_crash.dump

# Design docs (local only)
docs/*
!docs/stats.json

# Third-party skill sources
.opencode/skills-sources/

# Local .opencode skills (user/local-only, not shared upstream)
.opencode/skills/

# Local project skills (user 2026-08-31 钦定: 不进远程, 本地唯一权威位置)
# 必须带前导斜杠锚定仓库根：裸 `skills/` 会按任意深度匹配，连带吞掉 .agents/skills/ 等平台技能目录
/skills/
# 各平台技能目录（Copilot / Gemini CLI 约定），同样本地专用；显式钉住以免锚定后浮出为 untracked
.agents/skills/
# 技能注册表：登记的正是上面这些本地专用技能，且仓内无任何消费者（纯本机记账）⇒ 与 AGENTS.md 同族，不进库
skills-lock.json

# Generated assets
image/
apps/web/public/sprites/
apps/web/scripts/refactor-*

# Per-project DB directories (BUG-033: prevent source pollution)
.hiveweave/

# IDE / tool local state
.trae/
.agent-browser/
.e2e-sandbox/
.cursor/
.superpowers/

# Agent test playgrounds (nested git repos used by agent teams during tests —
# committing them creates broken gitlinks without .gitmodules)
sheep-game/
tictactoe-game/
tictactoe-game3/
.trae-html-share-packages
shoulabridge-audit

# Browser-automation artifacts (agent-browser/devtools downloads & temp files)
gargantua/
_dl_*.js
_lib_*.js
[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]

# 根目录下的裸数字文件名 —— shell 重定向误写（`2>1` 漏掉 `&`，stderr 被写进
# 名为 `1` 的文件；实测 `echo hi 2>1` 即产生 0 字节文件 `1`），或工具把某个值
# （如毫秒时间戳）当成了输出文件名。
# ⚠ 必须锚定 `/`：不锚定会匹配任意层级的数字开头名字，误伤 `docs/2026-09-04/`
# 这类合法子目录（2026-09-21 实证：`[0-9]*` 无锚会命中它）。
/[0-9]*

# Platform-issue research reports (本地归档，不进远程)
docs/platform-issue-research/

# Office scene working artifacts (备份图 & LaMa 修图中间产物；生产图无后缀已入库)
# ⚠ 真值源 = `apps/web/vite.config.ts::ARTIFACT_PATTERNS`（构建侧按它剔除
#   dist）。两处必须同源：漏写一条 ⇒ 文件进 git 且**同时**被打进 EXE 产物
#   （2026-09-17 审计发现的漂移点：`.pre-lama2.png` 无 `.bak` 那类此前两边
#   不一致）。新增一条中间产物命名时**两处都要加**。
# 美术工作文件（LaMa 修图中间产物，可从 assets/art/office/source/ + 脚本再生；约 8 MB 不进 git）
# 2026-09-22 从 apps/web/public/office-assets/ 迁至 assets/art/office/working/
# —— 搬迁动机：public/ 下一切都会进 dist/EXE，这里才是「不让中间产物进包」的根治（原先只靠构建脚本 /XF 止血）
assets/art/office/working/

# 同一精神，但针对**没搬走、仍留在 public/ 下**的那类：`public/` 会被原样拷进
# dist ⇒ 备份图与"仅作对照"的拆分中间产物在 public/ 下裸放的后果是
# **既不进 git 又会进 EXE**（两道防线同时失效）。
# ⚠ 下面 5 条与 `vite.config.ts::ARTIFACT_PATTERNS` **逐条对应** —— 加一条要两处都加。
#   2026-09-24 复查：此前只有 `.bak.png` 一条，`*.patched.png` / `*.pre-lama*.png`
#   侧只在构建侧拦（.gitignore 侧漏），`.pre-lama2).bak.png` 由 `*.bak.png` 覆盖、不另列。
*.bak.png
*.patched.png
*.pre-lama.png
*.pre-lama2.png
*-nofurniture.png

# Local AI memory backups (AI_MEMORY.local.md 已 ignore，备份同精神)
AI_MEMORY.local.md.bak*

# ── tasks/ —— **整个目录不入库**（2026-09-24 用户钦定）────────────────────
# 用户口径：「云仓库只放**项目必要的代码和资源**，开发过程文件不要」。
# 依据与证据：
#   · 它是美术加工 / 取证 / 一次性对照脚本的**工作区**。实测**无任何引用**：
#     `grep -rnE "(from|import) +tasks\.|../tasks/|tasks/[a-z_]+\.py"` 在
#     `apps/**`、`scripts/`、`.github/` 全为 0 命中（命中的都是 `services/tasks/`
#     `tools/tasks/` 这类**模块路径**，不是本目录）。
#   · 2026-09-24 之前已入库 49 个（09-06 曾钦定「复用管线入库」）+ 本轮新增 27 个，
#     共 76 个 ⇒ 本次一并移出：`git rm -r --cached tasks/`（**只动索引，磁盘文件全保留**）。
#   · ⚠ 代价（已就地处置）：`constants.ts` 注释引用过
#     `tasks/frontdesk-extract/split_*.py` / `tasks/hr-frontdesk/ref/hr-girl-front.png`
#     作为再生路径 ⇒ 那几处已标注「本地工作文件，未入库」，不再假装它随仓发布。
#   · 下面各条细规则在整目录移出后**成为冗余**，保留作历史判据记录（别急着删，
#     它们记录了 `.output.1` 轮转、`_desk_debug/` 等坑的成因）。
/tasks/

# tasks 工作产物：冻结 DB 快照 / 调试图目录 / tar 中转包 / 内联 three.js 的超大自检文件 / 提交信息草稿
tasks/_desk_debug/
tasks/_v45_snap/
tasks/_s310_app.tar.gz
tasks/_standalone_check.mjs
tasks/commit-msg-*.txt

# tasks 一次性验证/核查脚本与快照绑定档案（跑完即弃；复用管线 _desk_*/_s310_* 除外）
tasks/_v45_*
tasks/_p44_*
tasks/_ws_instruct*.py
tasks/check_recovery.py
tasks/_inline_module_check.mjs
tasks/_served_*
tasks/create-*-payload.json

# ComfyUI 节点 schema 快照（**生成物，勿入库**）：一条 `curl /object_info` 即可重导，
# 随本机 ComfyUI 版本变（09-10 那轮 2.4MB/1410 节点已删过一次、09-16 又出现 ⇒ 按名字防复发）。
# 需查节点参数时看 skills/comfyui-sprite-sheet/SKILL.md 的 GET /object_info，别把快照提交进来。
object_info.json

# 前台（frontdesk）提取/生成轮的工作目录（2026-09-23）—— 合计 28 MB 的探针帧、
# alpha 原图、sweep 采样与 H3 样片。**只 ignore 产物，脚本与 prompt 入库**：
# `.py` + `prompt_*.txt` 是"这张图怎么来的"的再生路径（constants.ts 直接引用了
# `split_frontdesk_layers.py` / `split_chair_piece.py` / `build_front_sheet.band_align`），
# 而产物一条命令可重跑 ⇒ 按 `.gitignore` 既有口径（脚本入库、快照不入库）分开处理。
# ⚠ 判据是**命名**不是"是不是 json/png"：带 `_` 前缀（`_sign_quad.json` / `_asset_*.png`）
#   判为探针中间数据；而无前缀的 `frontdesk_layers.json` / `sign_curve.json` 是
#   `split_frontdesk_layers.py` / `sign_name.py` 的**标定参数存档**（FRONT_SIGN 数值即源于此）
#   ⇒ 随脚本人库，故这里不写 `*.json`。
tasks/frontdesk-extract/*.png
tasks/frontdesk-extract/_*.json
tasks/hr-frontdesk/**/*.png
# ⚠ 反选必须排在**忽略规则之后**（gitignore 是"后匹配者胜"，写前面等于没写 ——
#   2026-09-24 首版就写错了，`git check-ignore` 实测仍是 IGNORED 才发现）。
# ⚠ 反选对象：`ref/hr-girl-front.png` 是**来源裁件**（不是产物）—— `constants.ts` 的
#   `AGENT_HR_FRONTDESK` 注释明文引用它（148×531），且 `resheet_qwen.py` /
#   `sweep_shots.py` 的 `--image` 默认值就是它；其上游立绘表不在仓内 ⇒
#   不跟进库会**断掉溯源链**（2026-09-24 审计发现，原规则把整个 ref/ 一锅端了）。
!tasks/hr-frontdesk/ref/hr-girl-front.png
tasks/hr-frontdesk/**/*.mp4
tasks/hr-frontdesk/**/_*.json
tasks/hr-frontdesk/runs/sweep/
