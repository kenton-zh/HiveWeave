# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
.next/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# PID files (runtime process IDs — machine-specific, must not be tracked)
*.pid

# Logs
*.log
npm-debug.log*
tasks/*.output

# Docker
.docker/

# SQLite
*.db
*.db-journal
*.db-wal
*.db-shm

# Test coverage
coverage/

# Temp
tmp/
temp/
tmp_*.py
tmp_*.txt
.tmp_*
_research/
.research/
.uploads/

# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Temp patch/fix scripts
_probe*.py
_fix*.mjs
_fix*.py
_fix*.cjs
_fix*.js
_patch*.py
_patch*.mjs
_patch*.cjs
_p[0-9]*.cjs
_pi.mjs
_apply_*.py
_apply_*.mjs
apply-app-patch.mjs
_ceo_*.txt
_t.txt
_inspect*.js
_inspect*.py
tasks/_diag_*

# Chat history exports (other projects)
*_chat_history.json
*_chat_history.md

# Empty temp files
SUMMARY.md
create_summary.py

# Temp debug files
check_db_tmp.ts

# Tool caches
.playwright-cli/
.turbo/
.workbuddy/

# Test fixtures
test-*.json
test_*.json

# Build diagnostics
tsc_err.txt
tsc_out.txt
*.tsbuildinfo
.npmrc

# Temp test artifacts
test_main_started.txt
test_output.txt
test-screenshots/
test-*/
test_*/
frontend-e2e-test.mjs
apps/web/frontend-e2e-test.mjs

# Cross-platform AI project memory (local only; do not push)
AI_MEMORY.local.md
*.local.md

# AI-agent instruction / tool docs (local working guidance, not project source)
AGENTS.md
CLAUDE.md
.claude/

# Local experiment/observation snapshots (复盘用，不入库)
observations/

# Local self-contained HTML analysis reports (bundle vendored echarts/mermaid/fonts)
reports/

# One-off per-project DB audit scripts (external absolute paths, snapshot-bound)
tasks/_tmp_*
tasks/diag_*.py
tasks/dsh*.py
tasks/dsh-watch-state.json
scripts/_tmp_*
apps/hiveweave-py/scripts/mechanism_attribution.py
apps/hiveweave-py/scripts/strip_reserved_tags.py

# Test postmortems / audit reports (process artifacts, local only)
deliverables/
audit_task_id_normalization.md

# Local self-contained competitive-analysis report site (vendored fonts/js)
hiveweave-competitive-analysis-2026/

# Misc
after-select.yaml
erl_crash.dump

# Design docs (local only)
docs/*
!docs/stats.json

# Third-party skill sources
.opencode/skills-sources/

# Local .opencode skills (user/local-only, not shared upstream)
.opencode/skills/

# Local project skills (user 2026-08-31 钦定: 不进远程, 本地唯一权威位置)
skills/

# Generated assets
image/
apps/web/public/sprites/
apps/web/scripts/refactor-*

# Per-project DB directories (BUG-033: prevent source pollution)
.hiveweave/

# IDE / tool local state
.trae/
.agent-browser/
.e2e-sandbox/
.cursor/
.superpowers/

# Agent test playgrounds (nested git repos used by agent teams during tests —
# committing them creates broken gitlinks without .gitmodules)
sheep-game/
tictactoe-game/
tictactoe-game3/
.trae-html-share-packages
shoulabridge-audit

# Browser-automation artifacts (agent-browser/devtools downloads & temp files)
gargantua/
_dl_*.js
_lib_*.js
[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]

# Platform-issue research reports (本地归档，不进远程)
docs/platform-issue-research/

# Office scene working artifacts (备份图 & LaMa 修图中间产物；生产图无后缀已入库)
apps/web/public/office-assets/*.bak.png
apps/web/public/office-assets/*.patched.png

# Local AI memory backups (AI_MEMORY.local.md 已 ignore，备份同精神)
AI_MEMORY.local.md.bak*

# tasks 工作产物：冻结 DB 快照 / 调试图目录 / tar 中转包 / 内联 three.js 的超大自检文件 / 提交信息草稿
tasks/_desk_debug/
tasks/_v45_snap/
tasks/_s310_app.tar.gz
tasks/_standalone_check.mjs
tasks/commit-msg-*.txt

# tasks 一次性验证/核查脚本与快照绑定档案（跑完即弃；复用管线 _desk_*/_s310_* 除外）
tasks/_v45_*
tasks/_p44_*
tasks/_ws_instruct*.py
tasks/check_recovery.py
tasks/_inline_module_check.mjs
tasks/_served_*
tasks/create-*-payload.json

# ComfyUI 节点 schema 快照（**生成物，勿入库**）：一条 `curl /object_info` 即可重导，
# 随本机 ComfyUI 版本变（09-10 那轮 2.4MB/1410 节点已删过一次、09-16 又出现 ⇒ 按名字防复发）。
# 需查节点参数时看 skills/comfyui-sprite-sheet/SKILL.md 的 GET /object_info，别把快照提交进来。
object_info.json
