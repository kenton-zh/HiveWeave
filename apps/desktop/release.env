# ============================================================
#  HiveWeave 分发配置模板（**提交态** · 零密钥 · 值显式钉死）
# ============================================================
#
#  构建期由 `build-exe.bat` 拷成 `<exe>/.env`（仅当那里还没有 .env 时）。
#  这是**产物平面**的文件，不是开发配置：
#    - 开发 `.env`（`apps/hiveweave-py/.env`）含 API 密钥，**永不进产物**；
#    - 本文件**零密钥**，可安全提交。
#
#  ## 为什么是「钉死值」而不是拷 `.env.example`
#
#  `.env.example` 把全部 tunable **注释掉** ⇒ 产物实际仍吃**代码默认值**，
#  而代码默认值随版本变（`constants.py` 里那些 `or "N"` 改一次，分发行为就
#  静默漂移一次，且**无人能 diff 出来**）。本文件把分发要跑的值**显式写出**，
#  于是「这一版分发跑什么数」是版本稳定、可 diff、可被冒烟断言对照的事实。
#
#  ## 取值依据
#
#  下列值 = 2026-09-11 时 `apps/hiveweave-py/src/hiveweave/llm/streamer/
#  constants.py` 的**代码默认值**（逐条注明行号），即"不写本文件时会吃什么"。
#  写出来 = 把它从隐式变显式（DSH packages/AGENTS.md:115「Explicit > implicit
#  at package boundaries」）。
#
#  ## 修改纪律
#
#  改这里 = 改分发行为。必须同时跑 `python scripts/smoke_release.py
#  --require-env`（该冒烟按**键级**逐个对照本文件与产物自报的
#  `effective_budget()`，缺键/不等即红）。
#
# ============================================================

# ── Turn 预算（秒）──────────────────────────────────────────
# 三者是**一组**：HARD 必须严格 < CEILING（constants.py:237 有结构断言，
# 满足不了会在 import 期就炸）。只调 HARD 不调 CEILING 是老坑。
#
# 软预算切片（constants.py:147 默认 540）
HIVEWEAVE_STREAM_TOTAL_TIMEOUT_S=540

# Turn 绝对硬上限（constants.py:158 默认 570）
HIVEWEAVE_STREAM_HARD_TIMEOUT_S=570

# 墙钟天花板，仅作「HARD 必须留余量」的断言参照（constants.py:171 默认 600）
HIVEWEAVE_STREAM_AGENT_CEILING_S=600

# ── LLM 并发 ────────────────────────────────────────────────
# 【单位不是秒：是「同时打开的 LLM 请求数」。】
# constants.py:313 默认 8。它对应**独立失败面**（吞吐 / provider 限流），
# 不被任何结构断言覆盖，且**有已记录事故**：`llm/streamer/__init__.py`
# 自陈 12 从未生效 ⇒ 13 次 hard timeout 烧约 123.5 分钟（≈总墙钟 40%）。
# 默认 8 是保守值（贴合 provider 并发限制）；调高前先确认配额。
HIVEWEAVE_LLM_MAX_CONCURRENT=8

# ── 单 turn 工具轮次疏导线（轮）─────────────────────────────
# 09-02 双触发修正：本线（轮数）与 FORCE_COMMIT_WALL_PCT（墙钟）**任一越线**
# 即进入疏导。轮数线抓快轮 churn，墙钟线抓慢轮无界磨。
# constants.py:30 默认 40。（曾一度被调到 400 当静音 —— 别这么干，
# 那是拿阈值的信噪比换安静。）
HIVEWEAVE_FORCE_COMMIT_ROUNDS=40

# ============================================================
#  以下项**有意留空**（不设 = 走代码路径的显式默认），不是遗漏：
#
#  - API 密钥类（HIVEWEAVE_ARK_API_KEY / HIVEWEAVE_OPENCODE_API_KEY /
#    HIVEWEAVE_DSH_API_KEY …）：**密钥绝不进产物**。分发形态下由用户在
#    「模型配置」UI 填写，落到 `<exe>/data/`（用户数据平面）。
#  - HIVEWEAVE_ARK_BASE_URL / MODEL_ID / CODING_*：同上，随用户在 UI 选择，
#    不钉死（换模型渠道不该需要重新出包）。
#  - HIVEWEAVE_DATA_ROOT：见 `.env.example` 注记 —— 它**不读 .env**
#    （config 直接查进程环境），写在这里是无声的谎。
#  - HIVEWEAVE_API_KEY（API 鉴权）：默认未设 = 开放；单体桌面分发即此语义。
#
#  要加新的钉死项：写在上面，并在 `scripts/smoke_release.py` 的 `CONFIG_KEYS`
#  里登记（键名必须与 `code_fingerprint.effective_budget()` 的键名逐字一致），
#  否则它是**只取证、无门禁价值**的。
# ============================================================
